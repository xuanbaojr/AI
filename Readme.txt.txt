Bài nộp của em training epochs = 1

Sau khi training xong với epochs = 20(model.h5), em lại chạy code training một lần nữa, sau đó pause
khi đó thì code nộp lên git sẽ xuất hiện đoạn mã lỗi khi em pause
(chỉ là hiện phần lỗi khi pause còn code vẫn hoạt động )
vì không còn thời gian nên em để epochs = 1 ở bài nộp

*Tuy nhiên thì phần output em sử dụng epochs = 20 (model.h5 và data.csv)
